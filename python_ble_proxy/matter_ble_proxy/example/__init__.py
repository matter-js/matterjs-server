"""Example / reference CLI for the matter-ble-proxy client library.

Mirrors `packages/ble-proxy/src/example/noble-ble-proxy.ts` from the JS side:
connects to a matter-server `/ble` endpoint and bridges a local Bleak adapter
into it for end-to-end testing of the BLE proxy protocol without Home
Assistant in the loop.
"""
